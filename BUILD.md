# 项目构建全流程指南

本项目采用多模块架构设计，构建过程分为四个核心步骤，确保各模块协同工作并生成最终可部署产物。以下是详细的构建说明：


## 🔧 1. 基础 POM 安装（必选前置步骤）

**作用**：将项目根目录的 `parent pom` 安装到本地 Maven 仓库，统一管理所有子模块的依赖版本、插件配置及全局属性，确保各模块使用一致的基础配置。

**执行命令**：
```bash
./mvnw install -N
```

> ⚠️ 注意：
> - 该命令仅安装父 POM（不构建子模块），`-N` 参数表示「不递归执行子模块」
> - 首次构建或父 POM 配置更新后必须执行，否则子模块可能因依赖缺失报错


## 🖥️ 2. 后端模块构建

**作用**：编译、打包后端各业务模块（如 `framework` 核心框架、`crm` 业务模块、`app` 接口模块等），并将构建结果安装到本地仓库，供其他模块依赖调用。

**执行命令**：
```bash
./mvnw clean install -DskipTests -DskipAntRunForJenkins --file backend/pom.xml
```

> ✅ 参数说明：
> - `clean`：清空之前的构建产物（`target` 目录），避免旧文件干扰
> - `install`：将构建好的 jar 包安装到本地 Maven 仓库（`~/.m2/repository`）
> - `-DskipTests`：跳过单元测试和集成测试（加快构建速度，正式发布前建议开启测试）
> - `-DskipAntRunForJenkins`：禁用 Jenkins 专属的 Ant 脚本任务（本地构建无需执行）
> - `--file backend/pom.xml`：指定后端模块的 POM 文件作为构建入口


## 💻 3. 前端模块构建

**作用**：编译前端资源（如 Vue/React 组件、JS/CSS 代码），生成可部署的静态文件（通常为 `dist` 目录），供后端模块引用或独立部署。

**前置要求**：
- 安装 Node.js（建议 v16+，可通过 `node -v` 验证）
- 安装依赖：进入 `frontend` 目录执行 `npm install`

**构建指南**：
详细步骤请参考前端模块专属文档：  
[`/frontend/README.md`](./frontend/README.md)（包含开发环境启动、生产环境打包、依赖版本要求等）

> 📌 提示：
> - 开发阶段可执行 `npm run dev` 启动热重载服务
> - 生产打包建议执行 `npm run build`，生成优化后的静态资源


## 📦 4. 整体项目打包（最终部署包生成）

**作用**：整合前后端构建结果，生成完整的可部署包（如包含所有依赖的 jar 包、Docker 镜像等），适用于测试环境或生产环境部署。

**执行命令**：
```bash
./mvnw clean package
```

> 📝 说明：
> - 该命令会递归执行所有模块的 `package` 生命周期，包括前端资源打包（若已配置前后端集成）
> - 构建成功后，最终产物通常位于根目录 `target` 或各子模块 `target` 目录下（如 `xxx.jar`、`xxx.zip`）
> - 如需跳过测试，可追加参数：`./mvnw clean package -DskipTests`


## 📋 构建流程图

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  1. 安装基础 POM  │────▶│  2. 后端模块构建  │────▶│  3. 前端模块构建  │────▶│  4. 整体项目打包  │
└─────────────────┘     └─────────────────┘     └─────────────────┘     └─────────────────┘
                                   │                     │                     │
                                   ▼                     ▼                     ▼
                           本地仓库依赖更新         前端静态资源生成         最终部署包输出
```


## ❗ 常见问题

1. **父 POM 未安装导致子模块报错**：重新执行步骤 1 即可。
2. **前端依赖安装失败**：检查 Node.js 版本，删除 `node_modules` 后重新执行 `npm install`。
3. **打包后缺少前端资源**：确保步骤 3 已成功执行，且后端 POM 中配置了前端资源复制逻辑。

如需进一步帮助，请参考项目根目录的 `TROUBLESHOOTING.md` 或联系开发团队。
